# CompanionBits Project

This project consists of a **frontend** (using React) and a **backend** (using Spring Boot).

## Getting Started

Follow the steps below to run the project locally.

---

## ⚙️ Prerequisites

Make sure you have the following installed on your machine:

- [Node.js](https://nodejs.org/) (for the frontend)
- [Maven](https://maven.apache.org/) (for the backend)
- [Java JDK 17+](https://adoptium.net/) (for Spring Boot)

---

## 🚀 Starting the Frontend

1. **Open the front-end folder in an integrated terminal**:

   - Right-click on the `front-end` folder in your code editor (e.g. VS Code).
   - Choose `Open in Integrated Terminal`.

2. **Install the dependencies**:

   ```bash
   npm install
   ```

3. **Start the development server**:

   ```bash
   npm run dev
   ```

## 🚀 Starting the Backend

1. **Open the back-end folder in an integrated terminal**:

   - Right-click on the `back-end` folder in your code editor (e.g. VS Code).
   - Choose `Open in Integrated Terminal`.

2. **Run the Spring Boot application using Maven**:

   ```bash
   mvn spring-boot:run
   ```
